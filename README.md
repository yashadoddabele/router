A dynamic, fully functional network router handling ARP, IP, and UDP packets. Implements the RIP routing protocol.

* Fully in C
* Tested with ICMP traceroute and ping commands
* Added functionality to interact with other routers by implementing the RIP protocol with split horizon and poison-reverse to avoid the count to infinity problem
* Managed and updated an RIP routing table
